## Packages
framer-motion | Smooth page transitions and micro-interactions for a premium feel
date-fns | Date formatting and manipulation
lucide-react | Beautiful, clean icons

## Notes
The API endpoints use Portuguese field names (numeroPedido, valorTotal, dataCriacao, items) as defined in orderInputSchema. The UI will display English labels but map correctly to these underlying fields when making mutations.
